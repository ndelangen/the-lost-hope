import { create as createItem } from '#/definitions/item.ts'

export default createItem({
  name: 'Phoenix Feather',
  icon: 'gi/GiFeather',
  currentOwner: null,
  carriedBy: null,
  craftedBy: null,
  notes: [['A luminous feather from a phoenix.']],
})
