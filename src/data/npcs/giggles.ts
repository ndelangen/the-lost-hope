import { create as createNPC } from '#/definitions/npc.ts'

export default createNPC({
  name: 'Giggles',
  species: 'Goblin',
  notes: [['A mysterious goblin whose identity and role remain unresolved.']],
})
